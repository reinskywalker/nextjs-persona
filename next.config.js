/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    basePath: '',
    trailingSlash: true,
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig